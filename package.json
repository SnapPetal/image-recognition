{
  "name": "image-recognition",
  "version": "0.1.0",
  "bin": {
    "image-recognition": "bin/image-recognition.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "cdk": "cdk",
    "test": "jest"
  },
  "devDependencies": {
    "aws-cdk": "2.76.0",
    "esbuild": "0",
    "jest": "^29.3.1"
  },
  "dependencies": {
    "aws-cdk-lib": "2.80.0",
    "constructs": "^10.0.0"
  }
}
